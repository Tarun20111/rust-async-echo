[package]
name = "echo-program-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
tokio = { version = "1.47", features = ["rt", "net", "io-util", "macros", "rt-multi-thread", "signal", "time"] }
